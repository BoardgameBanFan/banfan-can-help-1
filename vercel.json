{
  "buildCommand": "npm install --include=optional sharp && npm install --production=false && npm run build",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "installCommand": "npm install --production=false"
}
